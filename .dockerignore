# Production images are built from a temporary artifact-only context. Keep the
# repository context fail-closed too: credentials, nested env files, Maven
# settings, keys, keystores, local agent state, sources, and runtime overrides
# are all excluded unless they are one of the two explicitly allowed files.
**
!Dockerfile
!app.jar
