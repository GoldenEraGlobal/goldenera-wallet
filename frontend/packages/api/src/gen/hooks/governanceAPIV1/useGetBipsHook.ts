/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { RequestConfig, ResponseErrorConfig } from '../../.kubb/client'
import type { GetBipsOptions, GetBipsStatus200 } from '../../models/GetBips'
import type { QueryKey, QueryClient, QueryObserverOptions, UseQueryResult } from '@tanstack/react-query'
import { getBips } from '../../clients/getBips'
import { queryOptions, useQuery } from '@tanstack/react-query'

export const getBipsQueryKey = ({ query }: Omit<GetBipsOptions, 'headers'> = {}) => ["v1", { url: "/api/core/v1/governance/bips" }, ...(query ? [query] : [])] as const

type GetBipsQueryKey = ReturnType<typeof getBipsQueryKey>

export function getBipsQueryOptions({ query }: GetBipsOptions = {}, config: Partial<Omit<RequestConfig, 'path' | 'query' | 'body' | 'headers' | 'url'>> = {}) {
  const queryKey = getBipsQueryKey({ query })
  return queryOptions<GetBipsStatus200, ResponseErrorConfig<Error>, GetBipsStatus200, typeof queryKey>({
   queryKey,
   queryFn: async ({ signal }) => {
      const { data } = await getBips({ ...config, query, signal: config.signal ?? signal, throwOnError: true })
      return data
   },
  })
}

/**
 * @summary Get the BIP overview
 * {@link /api/core/v1/governance/bips}
 */
export function useGetBipsHook<TData = GetBipsStatus200, TQueryData = GetBipsStatus200, TQueryKey extends QueryKey = GetBipsQueryKey>({ query }: { query?: GetBipsOptions['query'] | (() => GetBipsOptions['query']) } = {}, options: {
  query?: Partial<QueryObserverOptions<GetBipsStatus200, ResponseErrorConfig<Error>, TData, TQueryData, TQueryKey>> & { client?: QueryClient },
  client?: Partial<Omit<RequestConfig, 'path' | 'query' | 'body' | 'headers' | 'url'>>
} = {}) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...resolvedOptions } = queryConfig
  const resolvedParams = { query: typeof query === 'function' ? query() : query }
  const queryKey = resolvedOptions?.queryKey ?? getBipsQueryKey(resolvedParams)

  const queryResult = useQuery({
   ...getBipsQueryOptions(resolvedParams, config),
   ...resolvedOptions,
   queryKey,
  } as unknown as QueryObserverOptions, queryClient) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey }

  queryResult.queryKey = queryKey as TQueryKey

  return queryResult
}
