/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { RequestConfig, ResponseErrorConfig } from '../../.kubb/client'
import type { GetBipsOptions, GetBipsStatus200 } from '../../models/GetBips'
import type { QueryKey, QueryClient, UseSuspenseQueryOptions, UseSuspenseQueryResult } from '@tanstack/react-query'
import { getBips } from '../../clients/getBips'
import { queryOptions, useSuspenseQuery } from '@tanstack/react-query'

export const getBipsSuspenseQueryKey = ({ query }: Omit<GetBipsOptions, 'headers'> = {}) => ["v1", { url: "/api/core/v1/governance/bips" }, ...(query ? [query] : [])] as const

type GetBipsSuspenseQueryKey = ReturnType<typeof getBipsSuspenseQueryKey>

export function getBipsSuspenseQueryOptions({ query }: GetBipsOptions = {}, config: Partial<Omit<RequestConfig, 'path' | 'query' | 'body' | 'headers' | 'url'>> = {}) {
  const queryKey = getBipsSuspenseQueryKey({ query })
  return queryOptions<GetBipsStatus200, ResponseErrorConfig<Error>, GetBipsStatus200, typeof queryKey>({
   queryKey,
   queryFn: async ({ signal }) => {
      const { data } = await getBips({ ...config, query, signal: config.signal ?? signal, throwOnError: true })
      return data
   },
  })
}

/**
 * @summary Get the BIP overview
 * {@link /api/core/v1/governance/bips}
 */
export function useGetBipsSuspenseHook<TData = GetBipsStatus200, TQueryKey extends QueryKey = GetBipsSuspenseQueryKey>({ query }: { query?: GetBipsOptions['query'] | (() => GetBipsOptions['query']) } = {}, options: {
  query?: Partial<UseSuspenseQueryOptions<GetBipsStatus200, ResponseErrorConfig<Error>, TData, TQueryKey>> & { client?: QueryClient },
  client?: Partial<Omit<RequestConfig, 'path' | 'query' | 'body' | 'headers' | 'url'>>
} = {}) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...resolvedOptions } = queryConfig
  const resolvedParams = { query: typeof query === 'function' ? query() : query }
  const queryKey = resolvedOptions?.queryKey ?? getBipsSuspenseQueryKey(resolvedParams)

  const queryResult = useSuspenseQuery({
   ...getBipsSuspenseQueryOptions(resolvedParams, config),
   ...resolvedOptions,
   queryKey,
  } as unknown as UseSuspenseQueryOptions, queryClient) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey }

  queryResult.queryKey = queryKey as TQueryKey

  return queryResult
}
