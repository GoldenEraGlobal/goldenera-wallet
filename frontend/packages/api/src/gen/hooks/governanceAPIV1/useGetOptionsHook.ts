/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { RequestConfig, ResponseErrorConfig } from '../../.kubb/client'
import type { GetOptionsStatus200 } from '../../models/GetOptions'
import type { QueryKey, QueryClient, QueryObserverOptions, UseQueryResult } from '@tanstack/react-query'
import { getOptions } from '../../clients/getOptions'
import { queryOptions, useQuery } from '@tanstack/react-query'

export const getOptionsQueryKey = () => ["v1", { url: "/api/core/v1/governance/options" }] as const

type GetOptionsQueryKey = ReturnType<typeof getOptionsQueryKey>

export function getOptionsQueryOptions(config: Partial<Omit<RequestConfig, 'path' | 'query' | 'body' | 'headers' | 'url'>> = {}) {
  const queryKey = getOptionsQueryKey()
  return queryOptions<GetOptionsStatus200, ResponseErrorConfig<Error>, GetOptionsStatus200, typeof queryKey>({
   queryKey,
   queryFn: async ({ signal }) => {
      const { data } = await getOptions({ ...config, signal: config.signal ?? signal, throwOnError: true })
      return data
   },
  })
}

/**
 * @summary Get current entities available to BIP creation forms
 * {@link /api/core/v1/governance/options}
 */
export function useGetOptionsHook<TData = GetOptionsStatus200, TQueryData = GetOptionsStatus200, TQueryKey extends QueryKey = GetOptionsQueryKey>(options: {
  query?: Partial<QueryObserverOptions<GetOptionsStatus200, ResponseErrorConfig<Error>, TData, TQueryData, TQueryKey>> & { client?: QueryClient },
  client?: Partial<Omit<RequestConfig, 'path' | 'query' | 'body' | 'headers' | 'url'>>
} = {}) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...resolvedOptions } = queryConfig
  const queryKey = resolvedOptions?.queryKey ?? getOptionsQueryKey()

  const queryResult = useQuery({
   ...getOptionsQueryOptions(config),
   ...resolvedOptions,
   queryKey,
  } as unknown as QueryObserverOptions, queryClient) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey }

  queryResult.queryKey = queryKey as TQueryKey

  return queryResult
}
