/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '../../../client'
import type { RequestConfig, ResponseErrorConfig } from '../../../client'
import type {
  GetNextNonceQueryResponse,
  GetNextNonceQueryParams,
  GetNextNonce400,
  GetNextNonce401,
  GetNextNonce404,
  GetNextNonce409,
  GetNextNonce413,
  GetNextNonce500,
} from '../../models/GetNextNonce'
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from '@tanstack/react-query'
import { queryOptions, useQuery } from '@tanstack/react-query'

export const getNextNonceQueryKey = (params: GetNextNonceQueryParams) =>
  ['v1', { url: '/api/core/v1/wallet/next-nonce' }, ...(params ? [params] : [])] as const

export type GetNextNonceQueryKey = ReturnType<typeof getNextNonceQueryKey>

/**
 * @description Get next nonce for a given address
 * @summary Get next nonce
 * {@link /api/core/v1/wallet/next-nonce}
 */
export async function getNextNonceHook(
  params: GetNextNonceQueryParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<
    GetNextNonceQueryResponse,
    ResponseErrorConfig<
      | GetNextNonce400
      | GetNextNonce401
      | GetNextNonce404
      | GetNextNonce409
      | GetNextNonce413
      | GetNextNonce500
    >,
    unknown
  >({ method: 'GET', url: `/api/core/v1/wallet/next-nonce`, params, ...requestConfig })
  return res.data
}

export function getNextNonceQueryOptionsHook(
  params: GetNextNonceQueryParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const queryKey = getNextNonceQueryKey(params)
  return queryOptions<
    GetNextNonceQueryResponse,
    ResponseErrorConfig<
      | GetNextNonce400
      | GetNextNonce401
      | GetNextNonce404
      | GetNextNonce409
      | GetNextNonce413
      | GetNextNonce500
    >,
    GetNextNonceQueryResponse,
    typeof queryKey
  >({
    enabled: !!params,
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal
      return getNextNonceHook(params, config)
    },
  })
}

/**
 * @description Get next nonce for a given address
 * @summary Get next nonce
 * {@link /api/core/v1/wallet/next-nonce}
 */
export function useGetNextNonceHook<
  TData = GetNextNonceQueryResponse,
  TQueryData = GetNextNonceQueryResponse,
  TQueryKey extends QueryKey = GetNextNonceQueryKey,
>(
  params: GetNextNonceQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetNextNonceQueryResponse,
        ResponseErrorConfig<
          | GetNextNonce400
          | GetNextNonce401
          | GetNextNonce404
          | GetNextNonce409
          | GetNextNonce413
          | GetNextNonce500
        >,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: typeof fetch }
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...queryOptions } = queryConfig
  const queryKey = queryOptions?.queryKey ?? getNextNonceQueryKey(params)

  const query = useQuery(
    {
      ...getNextNonceQueryOptionsHook(params, config),
      queryKey,
      ...queryOptions,
    } as unknown as QueryObserverOptions,
    queryClient
  ) as UseQueryResult<
    TData,
    ResponseErrorConfig<
      | GetNextNonce400
      | GetNextNonce401
      | GetNextNonce404
      | GetNextNonce409
      | GetNextNonce413
      | GetNextNonce500
    >
  > & { queryKey: TQueryKey }

  query.queryKey = queryKey as TQueryKey

  return query
}
